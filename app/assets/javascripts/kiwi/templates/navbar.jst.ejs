<div class="navbar-header">
  <button class="navbar-toggle" type="button" data-toggle="collapse" data-target=".navbar-collapse">
    <span class="icon-bar"></span>
    <span class="icon-bar"></span>
    <span class="icon-bar"></span>
  </button>
  <a href="../" class="navbar-brand">Forekast</a>
</div>

<div class="collapse navbar-collapse">
  <ul class="nav navbar-nav">
    <% if(username) { %>
      <li data-option="new">
        <a href="#/events/new" class="add-new">+ Add Event</a>
      </li>
      <% } %>
  </ul>
  <ul class="nav navbar-nav navbar-right">
    <% if (username) { %>
      <li>
        <a class="btn btn-focus" href="/users/edit"><%= username %></a>
      </li>
      <li>
        <a class="btn btn-alt" href="<%= FK.Links.logout %>">logout</a>
      </li>
    <% } else { %>
      <li><a href="<%= FK.Links.register %>" class="btn btn-focus" href="">Sign up</a></li>
      <li><a href="<%= FK.Links.login %>" class="btn btn-alt">Log in</a></li>
    <% } %>
  </ul>

<!--
  <ul class="nav navbar-nav navbar-right">
    <div class="btn-group">
      <button type="button" class="profile" data-toggle="dropdown">
        <%= username %><span class="caret"></span>
      </button>
      <ul class="dropdown-menu" role="menu">
        <li><a href="#">Account Settings</a></li>
        <li class="divider"></li>
        <li><a href="<%= FK.Links.logout %>">Logout</a></li>
      </ul>
    </div>
    <% if(username) { %>
    <% } else { %>
      <li>
        <a href="<%= FK.Links.login %>">login</a>
      </li>
      <li>
        <a href="<%= FK.Links.register %>">register</a>
      </li>
    <% } %>
  </ul>
</div>
--!>
