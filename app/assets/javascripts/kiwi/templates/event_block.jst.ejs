<h3><% if (isToday()) { %>Today, <% } %><%= date.format('MMMM D, YYYY') %></h3>
<div class="events">
</div>
<div class="button-container">
  <button class='btn btn-more'>Show More <i class="glyphicon glyphicon-chevron-down"></i></button>
</div>
